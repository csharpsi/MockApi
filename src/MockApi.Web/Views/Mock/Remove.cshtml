@model MockApi.Web.Models.Mock
@{
    ViewBag.Title = "Remove";
}

<h2>Remove Mock</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Mock Details</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Path)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Path)
        </dd>

        <dt>
            @Html.DisplayNameFor(x => x.Verb)
        </dt>
        <dd>
            @Html.DisplayFor(x => x.Verb)
        </dd>

        <dt>
            Configured responses
        </dt>

        <dd>
            @Model.Responses.Count
        </dd>

    </dl>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <button type="submit" class="btn btn-danger">I know what I'm doing, leave me alone</button>
            <button type="button" class="btn btn-primary" onclick="window.location.href='@Url.Action("Index")'">Cancel</button>
        </div>
    }
</div>

