@model MockApi.Web.ViewModels.MockResponseViewModel
@{
    ViewBag.Title = "RemoveStatus";
}

<h2>Remove Status</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Mock Response Status</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Mock.Path)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Mock.Path)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.MockResponse.StatusCode)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.MockResponse.StatusCode)
        </dd>

        <dt>
            @Html.DisplayNameFor(x => x.Mock.Verb)
        </dt>
        <dd>
            @Html.DisplayFor(x => x.Mock.Verb)
        </dd>

    </dl>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <button type="submit" class="btn btn-danger">I know what I'm doing, leave me alone</button>
            <button type="button" class="btn btn-primary" onclick="window.location.href='@Url.Action("Edit", new {id = Model.Mock.Id})'">Cancel</button>
        </div>
    }
</div>

